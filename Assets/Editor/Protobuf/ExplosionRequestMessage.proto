syntax = "proto3";
package Message;
import "Vector3Message.proto";

// 爆炸请求消息，C -> S
message ExplosionRequestMessage
{
	int32 clientDynamicItemId = 1;
	Vector3Message center = 2;
	float radius = 3;
}